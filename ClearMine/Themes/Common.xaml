<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:ClearMine.UI.Controls"
                    xmlns:con="clr-namespace:ClearMine.UI.Converters"
                    xmlns:logic="clr-namespace:ClearMine.Logic;assembly=ClearMine.Logic">
    <con:MathOperationConverter x:Key="MathOperationConverter" />
    <con:PlaygroundBorderThicknessConverter x:Key="PlaygroundBorderThicknessConverter" />
    <DataTemplate DataType="{x:Type logic:MineCell}">
        <local:MineCellControl />
    </DataTemplate>

    <ControlTemplate x:Key="ValidationErrorTemplate">
        <Border BorderBrush="#99FF0000"
                BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="4"
                                  Color="Red" ShadowDepth="0" />
            </Border.Effect>
            <AdornedElementPlaceholder />
        </Border>
    </ControlTemplate>
</ResourceDictionary>